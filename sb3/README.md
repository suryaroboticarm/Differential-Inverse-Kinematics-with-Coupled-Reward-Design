This directory stores Stable Baselines 3 (sb3) artifacts such as trained models, logs, and checkpoints.

Place SB3 model and training outputs here (e.g., `.zip` model files, tensorboard logs, and checkpoints).